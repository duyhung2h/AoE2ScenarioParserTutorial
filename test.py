
for a in range(0, len(listCharacters), 1):
    for b in range(0, len(listEquipments), 1):
        trigger1 = source_trigger_manager.add_trigger(" ")
    trigger1P1 = source_trigger_manager.copy_trigger(trigger_select=trigger1.trigger_id)